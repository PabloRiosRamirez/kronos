spring:
  application:
    name: kronos
logging:
  level:
    org:
      springframework:
        web: trace
eureka:
  instance:
    hostname: kronos-qa.herokuapp.com
  client:
    service-url:
      defaultZone: https://${eureka.instance.hostname}/eureka/
    healthcheck:
      enabled: true